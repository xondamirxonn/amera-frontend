"use client";
import { getCategoryId } from "@/services/getCategoryId";
import { useParams } from "next/navigation";
import React from "react";

export const SubcategorySingle = async () => {
  // const { id } = useParams();
  // const data = await getCategoryId(id);
  // console.log(data);

  return <div></div>;
};
